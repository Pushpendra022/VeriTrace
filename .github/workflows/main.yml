name: Keep backend awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Ping VeriTrace API
        run: curl --fail --retry 2 --max-time 90 https://veritrace-api.onrender.com/api/health
